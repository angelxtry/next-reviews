import { Heading } from '@/components/heading';

const metadata = {
  title: 'About',
};

const AboutPage = () => {
  return <Heading>About</Heading>;
};

export { metadata };
export default AboutPage;
